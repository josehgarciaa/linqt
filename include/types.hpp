#ifndef TYPES_HPP
#define TYPES_HPP

#include <cstdint>
#include <complex>
#include <Eigen/Core>

typedef Eigen:Index MatIdx;
typedef Eigen:Index MatSize;

typedef int32_t Int;
typedef double Real;
typedef std::complex<double> Complex;

#endif